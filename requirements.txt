# ==============================================================================
# ROEMAH ROTI - SYSTEM & ENVIRONMENT REQUIREMENTS
# ==============================================================================
# Note: This is a Next.js (Node.js) application. 
# Node.js manages runtime dependencies using 'package.json' (which is read 
# automatically by Railway and npm).
# Below are the system requirements and environment configurations needed to 
# run and deploy this application.
# ==============================================================================

# --- System Prerequisites ---
Node.js >= 18.x.x
npm >= 9.x.x
PostgreSQL >= 14.x.x (or any database supported by Prisma)

# --- Primary Dependencies (Auto-managed via package.json) ---
next == 16.2.10
react == 19.2.4
react-dom == 19.2.4
@prisma/client == 6.19.3
prisma == 6.0.0
whatsapp-web.js == 1.34.7
firebase == 12.15.0

# --- Required Environment Variables (Railway / Production) ---
# Configure these in your Railway service settings under "Variables"

# Prisma Database connection URL (e.g. PostgreSQL, MySQL, etc.)
DATABASE_URL=postgresql://<username>:<password>@<host>:<port>/<database_name>?sslmode=require

# Firebase Client configurations
NEXT_PUBLIC_FIREBASE_API_KEY=your_api_key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your_auth_domain
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your_project_id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your_storage_bucket
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
NEXT_PUBLIC_FIREBASE_APP_ID=your_app_id
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=your_measurement_id
NEXT_PUBLIC_FIREBASE_USE_EMULATORS=false
